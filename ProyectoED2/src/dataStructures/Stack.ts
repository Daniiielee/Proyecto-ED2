// Estructura de datos: Pila (Stack)
// Usada para: Historial de navegación (atrás/adelante)
// Esta es una estructura vacía que será implementada posteriormente

/**
 * Pila (Stack) - LIFO (Last In, First Out)
 * Último en entrar, primero en salir
 * Utilidad: Historial de navegación, deshacer acciones
 */
class Stack<T> {
  // items: T[];

  // Métodos a implementar:
  // - push(element: T): Agregar elemento al tope
  // - pop(): T | undefined: Remover y retornar elemento del tope
  // - peek(): T | undefined: Ver elemento del tope sin remover
  // - isEmpty(): boolean: Verificar si la pila está vacía
  // - size(): number: Obtener cantidad de elementos
  // - clear(): Limpiar la pila
  // - print(): Imprimir pila
}

export { Stack };
