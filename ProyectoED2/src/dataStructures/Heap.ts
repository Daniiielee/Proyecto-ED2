// Estructura de datos: Heap (Montículo)
// Usada para: Priorización de productos en búsqueda y recomendaciones
// Esta es una estructura vacía que será implementada posteriormente

/**
 * Heap (Montículo)
 * Árbol binario especializado que mantiene propiedad de heap
 * Utilidad: Priorización, búsqueda de máximos/mínimos rápida, recomendaciones
 */
class Heap<T> {
  // items: T[];

  // Métodos a implementar:
  // - insert(element: T): Insertar elemento
  // - extractMax(): T | undefined: Extraer elemento máximo
  // - extractMin(): T | undefined: Extraer elemento mínimo
  // - peek(): T | undefined: Ver elemento máximo sin remover
  // - size(): number: Obtener cantidad de elementos
  // - isEmpty(): boolean: Verificar si heap está vacío
  // - heapify(): Reorganizar heap
}

export { Heap };
