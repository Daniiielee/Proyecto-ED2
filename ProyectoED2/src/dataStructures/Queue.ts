// Estructura de datos: Cola (Queue)
// Usada para: Procesamiento de solicitudes y órdenes
// Esta es una estructura vacía que será implementada posteriormente

/**
 * Cola (Queue) - FIFO (First In, First Out)
 * Primero en entrar, primero en salir
 * Utilidad: Procesamiento de órdenes, cola de espera
 */
class Queue<T> {
  // items: T[];

  // Métodos a implementar:
  // - enqueue(element: T): Agregar elemento al final
  // - dequeue(): T | undefined: Remover y retornar elemento del frente
  // - front(): T | undefined: Ver elemento del frente sin remover
  // - isEmpty(): boolean: Verificar si la cola está vacía
  // - size(): number: Obtener cantidad de elementos
  // - clear(): Limpiar la cola
  // - print(): Imprimir cola
}

export { Queue };
